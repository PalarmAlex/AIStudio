<Window x:Class="AIStudio.Dialogs.AutomatizmLoadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Загрузка автоматизмов из файла" 
        Height="650" Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        PreviewKeyDown="Window_PreviewKeyDown"
        Loaded="Window_Loaded">

  <Window.Resources>
    <Style x:Key="DialogHeaderStyle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="16"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Margin" Value="0,0,0,15"/>
    </Style>

    <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
      <Setter Property="Margin" Value="0,10,0,5"/>
    </Style>

    <Style x:Key="InfoTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="#FF666666"/>
      <Setter Property="FontStyle" Value="Italic"/>
      <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="TabHeaderStyle" TargetType="TabItem">
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Padding" Value="15,5"/>
    </Style>

    <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
  </Window.Resources>

  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Заголовок -->
    <TextBlock Grid.Row="0" 
               Text="Загрузка автоматизмов из файла"
               Style="{StaticResource DialogHeaderStyle}"/>

    <!-- Таб контроль для переключения между режимами -->
    <TabControl Grid.Row="1" Margin="0,10,0,10" x:Name="MainTabControl">
      <!-- Вкладка загрузки -->
      <TabItem Header="Загрузка из файла" Style="{StaticResource TabHeaderStyle}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="0,10,0,0">
            <!-- Информация о файле -->
            <Border BorderBrush="#FFE0E0E0" 
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10"
                    Background="#FFF9F9F9"
                    Margin="0,0,0,15">
              <StackPanel>
                <TextBlock Text="Информация о файле" 
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>
                <TextBlock Text="{Binding FilePath, Mode=OneWay}"
                           TextWrapping="Wrap"
                           Style="{StaticResource InfoTextStyle}"/>
              </StackPanel>
            </Border>

            <!-- Выбор базового состояния -->
            <StackPanel Margin="0,0,0,15">
              <TextBlock Text="Базовое состояние агента"
                         Style="{StaticResource SectionHeaderStyle}"/>

              <ComboBox ItemsSource="{Binding BaseStates}"
                        DisplayMemberPath="Value"
                        SelectedValuePath="Key"
                        SelectedValue="{Binding SelectedBaseState}"
                        Height="30"
                        Width="250"
                        HorizontalAlignment="Left"
                        Margin="0,5,0,0"/>
            </StackPanel>

            <!-- Выбор комбинации стилей -->
            <StackPanel Margin="0,0,0,15">
              <TextBlock Text="Комбинация стилей поведения"
                         Style="{StaticResource SectionHeaderStyle}"/>

              <DockPanel Margin="0,5,0,0">
                <ComboBox ItemsSource="{Binding StyleCombinations}"
                          DisplayMemberPath="DisplayName"
                          SelectedValuePath="StyleIds"
                          SelectedValue="{Binding SelectedStyleIds}"
                          Height="30"
                          DockPanel.Dock="Left"
                          Width="500"/>

                <Button Content="Обновить список"
                        Command="{Binding LoadStylesCommand}"
                        Height="30"
                        Width="120"
                        Margin="10,0,0,0"
                        DockPanel.Dock="Right"/>
              </DockPanel>

              <TextBlock Text="{Binding StylesStatusText}"
                         Style="{StaticResource InfoTextStyle}"
                         Margin="0,5,0,0"
                         TextWrapping="Wrap"/>

              <!-- Индикатор выбора стилей -->
              <TextBlock Text="{Binding SelectedStyleValidationMessage}"
                         Foreground="{Binding SelectedStyleValidationColor}"
                         Margin="0,5,0,0"
                         FontSize="11"
                         Visibility="{Binding ShowStyleValidation, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>

            <!-- Предупреждение о необходимости выбора -->
            <Border BorderBrush="#FFF0E68C"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10"
                    Background="#FFFFFACD"
                    Margin="0,0,0,15"
                    Visibility="{Binding ShowSelectionWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
              <TextBlock Text="Для применения необходимо выбрать базовое состояние и комбинацию стилей"
                         Foreground="#8B4513"
                         FontWeight="SemiBold"/>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <!-- Вкладка редактирования CSV -->
      <TabItem Header="Редактирование CSV" Style="{StaticResource TabHeaderStyle}">
        <Grid Margin="0,10,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Информация о редактировании -->
          <Border Grid.Row="0"
                  BorderBrush="#FFE0E0E0" 
                  BorderThickness="1"
                  CornerRadius="4"
                  Padding="10"
                  Background="#FFF9F9F9"
                  Margin="0,0,0,10">
            <StackPanel>
              <TextBlock Text="Редактирование файла" 
                         FontWeight="SemiBold"
                         Margin="0,0,0,5"/>
              <TextBlock Text="{Binding FilePath, Mode=OneWay}"
                         TextWrapping="Wrap"
                         Style="{StaticResource InfoTextStyle}"/>
              <TextBlock TextWrapping="Wrap"
                         Style="{StaticResource InfoTextStyle}"
                         Margin="0,5,0,0">
                <Run Text="Формат файла:" FontWeight="Bold"/><LineBreak/>
                <Run Text="• Каждая строка содержит последовательность фраз"/><LineBreak/>
                <Run Text="• Фразы разделяются символом ';' или ' - '"/><LineBreak/>
                <Run Text="• Например: "/><Run Text="приветик;как спалось;отличненько" FontFamily="Consolas"/>
              </TextBlock>
            </StackPanel>
          </Border>

          <!-- Текстовое поле для редактирования CSV -->
          <TextBox Grid.Row="1"
                   x:Name="CsvEditorTextBox"
                   Text="{Binding CsvContent, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsReturn="True"
                   AcceptsTab="True"
                   FontFamily="Consolas"
                   FontSize="12"
                   VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Auto"
                   Padding="5"
                   BorderBrush="#FFE0E0E0"
                   BorderThickness="1"
                   IsEnabled="{Binding IsEditingEnabled}"/>

          <!-- Кнопки для редактирования -->
          <StackPanel Grid.Row="2"
                      Orientation="Horizontal"
                      HorizontalAlignment="Right"
                      Margin="0,10,0,0">
            <Button Content="Сохранить изменения"
                    Command="{Binding SaveCsvCommand}"
                    Height="35"
                    Width="150"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    ToolTip="Сохранить изменения в файл"/>

            <Button Content="Проверить формат"
                    Command="{Binding ValidateCsvCommand}"
                    Height="35"
                    Width="130"
                    Cursor="Hand"/>
          </StackPanel>
        </Grid>
      </TabItem>

      <!-- Вкладка редактирования промпта для ИИ -->
      <TabItem Header="Промпт для ИИ" Style="{StaticResource TabHeaderStyle}">
        <Grid Margin="0,10,0,0">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
          </Grid.RowDefinitions>

          <!-- Информация о промпте -->
          <Border Grid.Row="0"
                  BorderBrush="#FFE0E0E0" 
                  BorderThickness="1"
                  CornerRadius="4"
                  Padding="10"
                  Background="#FFF9F9F9"
                  Margin="0,0,0,10">
            <StackPanel>
              <TextBlock Text="Редактирование промпта для ИИ" 
                         FontWeight="SemiBold"
                         Margin="0,0,0,5"/>
              <TextBlock Text="{Binding PromptFilePath, Mode=OneWay}"
                         TextWrapping="Wrap"
                         Style="{StaticResource InfoTextStyle}"/>
              <TextBlock Text="Этот промпт используется при генерации содержимого файла цепочек диалога automatizm_generate_list_1.csv через ИИ. Вы можете отредактировать его по своему усмотрению."
                         Style="{StaticResource InfoTextStyle}"
                         Margin="0,5,0,0"
                         TextWrapping="Wrap"/>
            </StackPanel>
          </Border>

          <!-- Текстовое поле для редактирования промпта -->
          <TextBox Grid.Row="1"
                   x:Name="PromptEditorTextBox"
                   Text="{Binding PromptContent, UpdateSourceTrigger=PropertyChanged}"
                   AcceptsReturn="True"
                   AcceptsTab="True"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   VerticalScrollBarVisibility="Auto"
                   HorizontalScrollBarVisibility="Auto"
                   Padding="5"
                   BorderBrush="#FFE0E0E0"
                   BorderThickness="1"
                   IsEnabled="{Binding IsPromptEditingEnabled}"/>

          <!-- Кнопки для редактирования промпта -->
          <StackPanel Grid.Row="2"
                      Orientation="Horizontal"
                      HorizontalAlignment="Right"
                      Margin="0,10,0,0">
            <Button Content="Сохранить промпт"
                    Command="{Binding SavePromptCommand}"
                    Height="35"
                    Width="150"
                    Margin="0,0,10,0"
                    Cursor="Hand"
                    ToolTip="Сохранить изменения в файл промпта"/>
          </StackPanel>
        </Grid>
      </TabItem>
    </TabControl>

    <!-- Кнопки диалога -->
    <StackPanel Grid.Row="2" 
                Orientation="Horizontal" 
                HorizontalAlignment="Right"
                Margin="0,10,0,0">
      <Button Content="Применить" 
              Command="{Binding ApplyCommand}"
              IsEnabled="{Binding CanApply}"
              Height="35" 
              Width="120"
              Margin="0,0,10,0"
              Cursor="Hand"
              ToolTip="Применить выбранные параметры и запустить генерацию автоматизмов"/>

      <Button Content="Отмена" 
              Command="{Binding CancelCommand}"
              Height="35" 
              Width="100"
              Cursor="Hand"/>
    </StackPanel>
  </Grid>
</Window>