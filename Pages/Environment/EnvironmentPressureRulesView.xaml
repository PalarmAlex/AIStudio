<UserControl x:Class="AIStudio.Pages.SymbiontEnv.EnvironmentPressureRulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:AIStudio.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
  <UserControl.Resources>
    <converters:IntDictionaryToStringConverter x:Key="IntDictionaryToStringConverter"/>
    <converters:ListToStringConverter x:Key="ListToStringConverter"/>
  </UserControl.Resources>
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding CurrentAgentTitle}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
    <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,8" Foreground="#FF333333"
               Text="Правила давления метрик среды (ProbeKey) на параметры гомеостаза. Применяются на пульсе, не отображаются на пульте как кнопки стимулов."/>

    <DataGrid x:Name="RulesGrid" Grid.Row="2" ItemsSource="{Binding Rules}"
              AutoGenerateColumns="False"
              IsReadOnly="{Binding IsReadOnlyMode}"
              CanUserAddRows="{Binding IsEditingEnabled}"
              CanUserDeleteRows="{Binding IsEditingEnabled}"
              SelectionMode="Extended"
              AddingNewItem="RulesGrid_AddingNewItem"
              PreviewKeyDown="RulesGrid_PreviewKeyDown">
      <DataGrid.Columns>
        <DataGridTextColumn Header="RuleId" Binding="{Binding RuleId, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
        <DataGridTemplateColumn Header="ProbeKey" Width="260">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <ComboBox ItemsSource="{Binding DataContext.ProbeKeyOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        SelectedValue="{Binding ProbeKey, Mode=OneWay}"
                        SelectedValuePath="Key"
                        DisplayMemberPath="DisplayText"
                        IsHitTestVisible="False"
                        Focusable="False"
                        BorderThickness="0"
                        Background="Transparent"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
          <DataGridTemplateColumn.CellEditingTemplate>
            <DataTemplate>
              <ComboBox ItemsSource="{Binding DataContext.ProbeKeyOptions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                        SelectedValue="{Binding ProbeKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        SelectedValuePath="Key"
                        DisplayMemberPath="DisplayText"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellEditingTemplate>
        </DataGridTemplateColumn>
        <DataGridTextColumn Header="Имя" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
        <DataGridTextColumn Header="Описание" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
        <DataGridTextColumn Header="Influences"
                            Binding="{Binding Influences, Converter={StaticResource IntDictionaryToStringConverter}}"
                            IsReadOnly="True" Width="180">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <EventSetter Event="MouseDown" Handler="InfluencesCell_MouseDown"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header="Antagonists"
                            Binding="{Binding Antagonists, Converter={StaticResource ListToStringConverter}}"
                            IsReadOnly="True" Width="120">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <EventSetter Event="MouseDown" Handler="AntagonistsCell_MouseDown"/>
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
      </DataGrid.Columns>
    </DataGrid>

    <Grid Grid.Row="3" Margin="0,8,0,0">
      <TextBlock Text="{Binding PulseWarningMessage}" Foreground="{Binding WarningMessageColor}" VerticalAlignment="Center"/>
      <Button Content="Сохранить" HorizontalAlignment="Right" Padding="12,6"
              Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditingEnabled}"/>
    </Grid>
  </Grid>
</UserControl>
