<UserControl x:Class="AIStudio.Pages.SymbiontEnv.EnvironmentRecipesRegistryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding CurrentAgentTitle}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>

    <Border Grid.Row="1" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,8" Background="#FFF5F5F5">
      <StackPanel Orientation="Horizontal">
        <Button Content="Обновить с диска" Padding="10,6" Margin="0,0,8,0" Command="{Binding RefreshCommand}"/>
        <Button Content="Новый рецепт" Padding="10,6" Command="{Binding NewCommand}" IsEnabled="{Binding IsEditingEnabled}"/>
      </StackPanel>
    </Border>

    <Border Grid.Row="2" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8" Background="#FFF9FBE7">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="120"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="ID" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
        <TextBox Grid.Column="1" Text="{Binding FilterIdText, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" Margin="0,0,16,0"/>
        <TextBlock Grid.Column="2" Text="Название" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
        <TextBox Grid.Column="3" Text="{Binding FilterTitleText, UpdateSourceTrigger=PropertyChanged}" Padding="4,3" Margin="0,0,16,0"/>
        <Button Grid.Column="4" Content="Применить" Padding="12,6" Margin="0,0,8,0" Command="{Binding ApplyFiltersCommand}"/>
        <Button Grid.Column="5" Content="Сброс" Padding="12,6" Command="{Binding ResetFiltersCommand}"/>
      </Grid>
    </Border>

    <DataGrid Grid.Row="3" ItemsSource="{Binding Items}" SelectedItem="{Binding Selected, Mode=TwoWay}"
              AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended"
              CanUserAddRows="False" CanUserDeleteRows="False"
              MouseDoubleClick="DataGrid_OnMouseDoubleClick" PreviewKeyDown="DataGrid_OnPreviewKeyDown">
      <DataGrid.Columns>
        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="180"/>
        <DataGridTextColumn Header="Название" Binding="{Binding DisplayName}" Width="*"/>
        <DataGridTextColumn Header="G_AD" Binding="{Binding AdaptiveActionId}" Width="60"/>
        <DataGridTextColumn Header="Риск" Binding="{Binding RiskTier}" Width="50"/>
      </DataGrid.Columns>
    </DataGrid>

    <Border Grid.Row="4" Padding="8,6" Margin="0,8,0,0">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Редактировать" Padding="10,6" Margin="0,0,8,0" Command="{Binding EditCommand}"/>
        <Button Content="Копировать" Padding="10,6" Command="{Binding DuplicateCommand}"/>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
