<UserControl x:Class="AIStudio.Pages.SymbiontEnv.EnvironmentTriggersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="{Binding CurrentAgentTitle}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
    <Border Grid.Row="1" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,0,0,8" Background="#FFF9FBE7">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="120"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="ID" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="SemiBold"/>
        <TextBox Grid.Column="1" Text="{Binding FilterIdText, UpdateSourceTrigger=PropertyChanged}" Padding="4,3"/>
        <TextBlock Grid.Column="2" Text="Название" VerticalAlignment="Center" Margin="16,0,8,0" FontWeight="SemiBold"/>
        <TextBox Grid.Column="3" Text="{Binding FilterTitleText, UpdateSourceTrigger=PropertyChanged}" Padding="4,3"/>
        <Button Grid.Column="4" Content="Применить" Padding="12,6" Margin="16,0,8,0" Command="{Binding ApplyFiltersCommand}"/>
        <Button Grid.Column="5" Content="Сброс" Padding="12,6" Command="{Binding ResetFiltersCommand}"/>
      </Grid>
    </Border>

    <DataGrid x:Name="TriggersGrid" Grid.Row="2" ItemsSource="{Binding Triggers}"
              AutoGenerateColumns="False"
              IsReadOnly="{Binding IsReadOnlyMode}"
              CanUserAddRows="{Binding IsEditingEnabled}"
              CanUserDeleteRows="{Binding IsEditingEnabled}"
              SelectionMode="Extended"
              AddingNewItem="TriggersGrid_AddingNewItem"
              PreviewKeyDown="TriggersGrid_PreviewKeyDown">
      <DataGrid.Columns>
        <DataGridTextColumn Header="ID" Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
        <DataGridTextColumn Header="Название" Binding="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
        <DataGridTemplateColumn Header="Воздействие" Width="100">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding InfluenceActionId}" Padding="6,4"
                         MouseDown="InfluenceActionCell_MouseDown"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn Header="Detect" Width="*">
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding DetectSummary}" Padding="6,4" ToolTip="Двойной щелчок — правила detect"
                         MouseDown="DetectCell_MouseDown"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>

    <Grid Grid.Row="3" Margin="0,8,0,0">
      <TextBlock Text="{Binding PulseWarningMessage}" Foreground="{Binding WarningMessageColor}" VerticalAlignment="Center"/>
      <Button Content="Сохранить" HorizontalAlignment="Right" Padding="12,6"
              Command="{Binding SaveCommand}" IsEnabled="{Binding IsEditingEnabled}"/>
    </Grid>
  </Grid>
</UserControl>
