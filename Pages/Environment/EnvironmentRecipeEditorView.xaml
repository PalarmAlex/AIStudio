<UserControl x:Class="AIStudio.Pages.SymbiontEnv.EnvironmentRecipeEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:env="clr-namespace:AIStudio.ViewModels.SymbiontEnv"
             xmlns:controls="clr-namespace:AIStudio.Controls.Environment"
             xmlns:converters="clr-namespace:AIStudio.Converters"
             MinHeight="400">
  <UserControl.Resources>
    <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
  </UserControl.Resources>

  <Grid Margin="4">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
      <Button Content="← К реестру" Padding="10,6" Margin="0,0,10,0" Click="BackButton_Click"/>
      <TextBlock Text="{Binding EditorTitle}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
      <Button Content="Основное" Padding="12,6" Margin="0,0,4,0" Click="TabGeneral_Click"/>
      <Button Content="Шаги" Padding="12,6" Margin="0,0,4,0" Click="TabSteps_Click"/>
      <Button Content="Связи" Padding="12,6" Margin="0,0,4,0" Click="TabLinks_Click"/>
    </StackPanel>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
      <StackPanel>
        <StackPanel x:Name="GeneralPanel">
          <StackPanel DataContext="{Binding Model}" IsEnabled="{Binding DataContext.IsEditingEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <TextBlock Text="ID" FontWeight="SemiBold"/>
            <Grid Margin="0,4,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <TextBox Grid.Column="0" Text="{Binding DataContext.RecipeId, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"/>
              <Button Grid.Column="1" Content="Выбрать…" Margin="8,0,8,0" Padding="10,4" Click="PickRecipeId_Click"/>
              <TextBlock Grid.Column="2" Text="{Binding DataContext.RecipeIdDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#FF555555"/>
            </Grid>
            <TextBlock Text="Название" FontWeight="SemiBold"/>
            <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,8" TextChanged="ModelField_TextChanged"/>
            <TextBlock Text="Описание" FontWeight="SemiBold"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="48" Margin="0,4,0,8" TextChanged="ModelField_TextChanged"/>
            <TextBlock Text="Моторное действие (G_AD)" FontWeight="SemiBold"/>
            <Grid Margin="0,4,0,8">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <TextBox Grid.Column="0" Text="{Binding DataContext.AdaptiveActionDisplay, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" IsReadOnly="True"/>
              <Button Grid.Column="1" Content="Выбрать…" Margin="8,0,8,0" Padding="10,4" Click="PickAdaptiveAction_Click"/>
              <TextBlock Grid.Column="2" Text="{Binding DataContext.AdaptiveActionDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#FF555555"/>
            </Grid>
            <CheckBox Content="reactive_eligible" IsChecked="{Binding ReactiveEligible}" Margin="0,0,0,8" Checked="ModelField_Changed" Unchecked="ModelField_Changed"/>
            <TextBlock Text="Рекомендуемый триггер (EA)" FontWeight="SemiBold"/>
            <Grid Margin="0,4,0,0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <TextBox Grid.Column="0" Text="{Binding DataContext.RecommendedTriggerDisplay, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" IsReadOnly="True"/>
              <Button Grid.Column="1" Content="Выбрать…" Margin="8,0,8,0" Padding="10,4" Click="PickRecommendedTriggers_Click"/>
              <TextBlock Grid.Column="2" Text="{Binding DataContext.RecommendedTriggerDescription, RelativeSource={RelativeSource AncestorType=UserControl}}" VerticalAlignment="Center" TextWrapping="Wrap" Foreground="#FF555555"/>
            </Grid>
          </StackPanel>
        </StackPanel>

        <StackPanel x:Name="StepsPanel" Visibility="Collapsed">
          <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
            <Button Content="+ Шаг" Command="{Binding AddInvokeStepCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="+ Комментарий" Command="{Binding AddCommentStepCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="▲" Command="{Binding MoveStepUpCommand}" Padding="8,4" Margin="0,0,4,0"/>
            <Button Content="▼" Command="{Binding MoveStepDownCommand}" Padding="8,4" Margin="0,0,8,0"/>
            <Button Content="Удалить" Command="{Binding DeleteSelectedStepsCommand}" Style="{StaticResource RedButtonStyle}" Padding="10,4"/>
          </StackPanel>
          <DataGrid x:Name="StepsGrid"
                    ItemsSource="{Binding Model.Steps}"
                    SelectedItem="{Binding SelectedStep, Mode=TwoWay}"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    SelectionMode="Single"
                    IsReadOnly="True"
                    BorderBrush="#FFE0E0E0"
                    BorderThickness="1"
                    MinHeight="140"
                    Margin="0,0,0,8"
                    LoadingRow="StepsGrid_LoadingRow">
            <DataGrid.Columns>
              <DataGridTextColumn Header="№" Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=Header}" Width="40"/>
              <DataGridTextColumn Header="Действие" Binding="{Binding DisplayAction}" Width="200"/>
              <DataGridTextColumn Header="Кратко" Binding="{Binding Summary}" Width="*"/>
            </DataGrid.Columns>
          </DataGrid>
          <controls:SchemaActionPanel DataContext="{Binding StepHandlerSchema}" MinHeight="200"/>
        </StackPanel>

        <StackPanel x:Name="LinksPanel" Visibility="Collapsed">
          <ItemsControl ItemsSource="{Binding Links}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border BorderBrush="#FFE8E8E8" BorderThickness="0,0,0,1" Padding="4">
                  <TextBlock TextWrapping="Wrap">
                    <Run Text="{Binding Category, Mode=OneWay}" FontWeight="SemiBold"/>
                    <Run Text=": "/>
                    <Run Text="{Binding Title, Mode=OneWay}"/>
                    <Run Text=" — "/>
                    <Run Text="{Binding Detail, Mode=OneWay}"/>
                  </TextBlock>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="→ Триггеры" Command="{Binding NavigateToTriggerCommand}" Padding="10,4" Margin="0,0,8,0"/>
            <Button Content="→ Рефлексы" Command="{Binding NavigateToReflexesCommand}" Padding="10,4"/>
          </StackPanel>
        </StackPanel>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>
