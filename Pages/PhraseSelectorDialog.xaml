<Window x:Class="AIStudio.Dialogs.PhraseSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PhraseSelectorDialog" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        SizeToContent="WidthAndHeight">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Фильтр -->
    <TextBlock Grid.Row="0" Text="Фильтр:" Margin="0,0,0,5"/>
    <TextBox Grid.Row="1" x:Name="FilterTextBox" 
                 TextChanged="FilterTextBox_TextChanged"
                 Margin="0,0,0,10"
                 Height="25"/>

    <!-- Список фраз -->
    <TextBlock Grid.Row="2" Text="Список фраз:" Margin="0,0,0,5"/>
    <ListBox Grid.Row="3" x:Name="PhrasesListBox" 
                 Height="300" 
                 MouseDoubleClick="PhrasesListBox_MouseDoubleClick"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
      <ListBox.ItemTemplate>
        <DataTemplate>
          <StackPanel Margin="2">
            <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>

    <!-- Статус -->
    <TextBlock Grid.Row="4" x:Name="StatusTextBlock" Margin="0,5,0,10"/>

    <!-- Кнопки -->
    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Очистить" Click="ClearButton_Click" Margin="0,0,10,0" Style="{StaticResource RedButtonStyle}" Width="80"/>
      <Button Content="OK" Click="OKButton_Click" Margin="0,0,10,0" Width="80" Style="{StaticResource ActionButtonStyle}" IsDefault="True"/>
      <Button Content="Отмена" Click="CancelButton_Click" Width="80" Style="{StaticResource ActionButtonStyle}" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>