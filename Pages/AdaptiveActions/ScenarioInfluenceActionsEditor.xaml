<Window x:Class="AIStudio.Dialogs.ScenarioInfluenceActionsEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="480" Width="440"
        WindowStartupLocation="CenterOwner"
        MinHeight="360" MinWidth="380"
        ResizeMode="CanResizeWithGrip"
        PreviewKeyDown="Window_PreviewKeyDown"
        Closing="Window_Closing">

  <Window.Resources>
    <Style x:Key="InfoTitleStyle" TargetType="TextBlock">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
  </Window.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
            Background="#FF2196F3"
            CornerRadius="4"
            Margin="0,0,0,10"
            Padding="10">
      <TextBlock x:Name="HeaderTitle"
                 Text="Воздействия по шагу"
                 FontSize="16"
                 FontWeight="Bold"
                 HorizontalAlignment="Center"
                 Foreground="White"/>
    </Border>

    <TextBlock Grid.Row="1"
               Text="Отметьте воздействия с пульта для этого шага:"
               Margin="0,0,0,8"
               Style="{StaticResource InfoTitleStyle}"
               Foreground="#FF0D47A1"/>

    <Border Grid.Row="2"
            BorderBrush="#FFE0E0E0"
            BorderThickness="1"
            CornerRadius="4">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#FFE8EAF6" BorderBrush="#FFE0E0E0" BorderThickness="0,0,0,1" Padding="8">
          <TextBlock Text="ВОЗДЕЙСТВИЯ" FontWeight="Bold" FontSize="14" Foreground="#FF3F51B5"/>
        </Border>
        <ListBox Grid.Row="1"
                 x:Name="ActionsList"
                 SelectionMode="Multiple"
                 Background="#FFF9F9F9"
                 BorderThickness="0">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <CheckBox Content="{Binding Name}"
                        IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        IsEnabled="{Binding IsEnabled}"
                        ToolTip="{Binding Description}"
                        Checked="CheckBox_Checked"
                        Unchecked="CheckBox_Unchecked"
                        Margin="4,2"/>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Grid>
    </Border>

    <Border x:Name="ConflictMessagePanel" Grid.Row="3"
            Background="#FFF0F0F0" BorderBrush="#FFCCCCCC" BorderThickness="1"
            Margin="0,10,0,0" Padding="6" Visibility="Collapsed">
      <TextBlock x:Name="ConflictMessage" TextWrapping="Wrap" Foreground="Red"/>
    </Border>

    <Border Grid.Row="4"
            BorderBrush="#FFE0E0E0"
            BorderThickness="1"
            CornerRadius="4"
            Padding="10"
            Margin="0,12,0,0"
            Background="#FFF5F5F5">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="OK" Width="100" Height="32" Margin="0,0,8,0"
                IsDefault="True" Click="OK_Click"
                Background="#FF4CAF50" BorderBrush="#FF388E3C" Foreground="White" FontWeight="SemiBold"/>
        <Button Content="Отмена" Width="100" Height="32" Click="Cancel_Click"
                Background="#FFF5F5F5" BorderBrush="#FF9E9E9E"/>
      </StackPanel>
    </Border>
  </Grid>
</Window>
