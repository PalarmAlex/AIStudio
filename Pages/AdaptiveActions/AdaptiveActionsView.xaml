<UserControl x:Class="AIStudio.Pages.AdaptiveActionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:AIStudio.Converters"
             xmlns:common="clr-namespace:AIStudio.Common" 
             xmlns:viewmodels="clr-namespace:AIStudio.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:AdaptiveActionsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

  <UserControl.Resources>
    <converters:ListToStringConverter x:Key="ListToStringConverter"/>
    <converters:IntDictionaryToStringConverter x:Key="IntDictionaryToStringConverter"/>
    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>

    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
      <Setter Property="Background" Value="White"/>
      <Style.Triggers>
        <Trigger Property="Validation.HasError" Value="true">
          <Setter Property="Background" Value="#FFFDE6E6"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
      <Setter Property="Background" Value="White"/>
      <Style.Triggers>
        <Trigger Property="Validation.HasError" Value="true">
          <Setter Property="Background" Value="#FFFDE6E6"/>
        </Trigger>
      </Style.Triggers>
    </Style>

  </UserControl.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Заголовок -->
    <TextBlock Grid.Row="0" 
               Text="{Binding CurrentAgentTitle}" 
               FontSize="18"
               FontWeight="SemiBold"
               HorizontalAlignment="Center"
               Margin="0,0,0,5"
               Foreground="#FF333333"/>

    <TextBlock Grid.Row="1" 
           Text="{Binding CurrentAgentDescription}"
           FontSize="12"
           HorizontalAlignment="Left"
           Margin="0,0,0,5"
           Foreground="#FF333333"
           TextWrapping="Wrap"
           Height="Auto"/>

    <Border Grid.Row="2" 
            BorderBrush="#FFE0E0E0" 
            BorderThickness="1"
            CornerRadius="4"
            Margin="0,0,0,5">
      <DataGrid ItemsSource="{Binding AdaptiveActions}"
                x:Name="ActionsGrid"
                IsEnabled="{Binding IsEditingEnabled}"
                IsReadOnly="{Binding IsEditingEnabled, Converter={StaticResource InverseBooleanConverter}}"
                AutoGenerateColumns="False"
                CanUserAddRows="True"
                SelectionMode="Extended"
                BorderThickness="0"
                Background="White"
                RowBackground="#FFF9F9F9"
                AlternatingRowBackground="White"
                GridLinesVisibility="None"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto"
                PreviewKeyDown="ActionsGrid_PreviewKeyDown"
                RowEditEnding="DataGrid_RowEditEnding"
                AddingNewItem="DataGrid_AddingNewItem"
                CellEditEnding="DataGrid_CellEditEnding">
        <DataGrid.Columns>

          <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" IsReadOnly="True"/>
          <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="150"/>

          <DataGridTextColumn Header="Описание" Binding="{Binding Description, Mode=TwoWay}" Width="300">
            <DataGridTextColumn.CellStyle>
              <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                <Setter Property="ToolTip" Value="{Binding Description}"/>
                <EventSetter Event="MouseDoubleClick" Handler="DescriptionCell_DoubleClick"/>
              </Style>
            </DataGridTextColumn.CellStyle>
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                <Setter Property="ToolTip" Value="ДБ клик открывает окно ввода"/>
              </Style>
            </DataGridTextColumn.HeaderStyle>
          </DataGridTextColumn>

          <DataGridTextColumn Header="Энергичность" Binding="{Binding Vigor, Mode=TwoWay}" Width="60">
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                        Интенсивность действия — уровень активности, скорости, физической нагрузки.
                        Используется для модуляции силы реакции через поведенческие стили.
                        Не определяет тип действия, только его "масштаб".
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
            <DataGridTextColumn.EditingElementStyle>
              <Style TargetType="TextBox">
                <EventSetter Event="PreviewTextInput" Handler="NumericColumn_PreviewTextInput"/>
              </Style>
            </DataGridTextColumn.EditingElementStyle>
          </DataGridTextColumn>

          <DataGridTemplateColumn Width="70">
            <DataGridTemplateColumn.Header>
              <TextBlock Text="Элементарное" 
                   ToolTipService.ShowDuration="30000">
                <TextBlock.ToolTip>
                  <TextBlock TextWrapping="Wrap" Width="200"
                           Text="Признак того, что действие является самым примитивным, запускаемым только напрямую по факту состояния &quot;Плохо&quot; 
                             параметров гомеостаза. Предназначенно исключительно для прямой компенсации параметров гомеостаза. 
                             Такие действия могут использоваться системой самоподдержания напрямую. ДБ клик открывает окно ввода."/>
                </TextBlock.ToolTip>
              </TextBlock>
          </DataGridTemplateColumn.Header>
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <CheckBox IsChecked="{Binding IsElementary}"
                    IsHitTestVisible="False"
                    Focusable="False"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
          <DataGridTemplateColumn.CellEditingTemplate>
            <DataTemplate>
              <CheckBox IsChecked="{Binding IsElementary}"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"/>
            </DataTemplate>
          </DataGridTemplateColumn.CellEditingTemplate>
        </DataGridTemplateColumn>

          <DataGridTextColumn Header="Влияния" 
                              Binding="{Binding Influences, Converter={StaticResource IntDictionaryToStringConverter}}" 
                              IsReadOnly="True" 
                              Width="*">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <EventSetter Event="MouseDown" Handler="InfluencesCell_MouseDown"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                        Влияние действия на параметры гомеостаза (положительное или отрицательное).
                        Ключ - ID параметра, значение - величина воздействия.
                        ДБ клик открывает окно ввода".
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
          </DataGridTextColumn>

          <DataGridTextColumn Header="Затраты" 
            Binding="{Binding Costs, Converter={StaticResource IntDictionaryToStringConverter}}" 
            IsReadOnly="True" 
            Width="*">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <EventSetter Event="MouseDown" Handler="CostsCell_MouseDown"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                      Затраты действия на параметры гомеостаза, например Энергия.
                      Ключ - ID параметра, значение - величина ухудшения (знак влияния зависит от типа параметра: дефицит или избыток ориентированный)
                      ДБ клик открывает окно ввода.
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
          </DataGridTextColumn>

          <DataGridTextColumn Header="К. усталости" Binding="{Binding FatigueCoefficient}" Width="70">
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                      Показывает, насколько снижается энергичность при каждом применении в % от текущей энергичности за применение.
                      Значение 0 отключает систему усталости.
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
            <DataGridTextColumn.CellStyle>
              <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                <EventSetter Event="PreviewTextInput" Handler="FatigueCoefficient_PreviewTextInput"/>
                <EventSetter Event="LostFocus" Handler="FatigueCoefficient_LostFocus"/>
              </Style>
            </DataGridTextColumn.CellStyle>
          </DataGridTextColumn>

          <DataGridTextColumn Header="К. восстановления" Binding="{Binding RecoveryCoefficient}" Width="70">
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                      Скорость возврата к базовой энергичности за секунду в % от базовой энергичности в секунду.
                      Значение 1 обеспечивает мгновенное восстановление.
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
            <DataGridTextColumn.CellStyle>
              <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                <EventSetter Event="PreviewTextInput" Handler="RecoveryCoefficient_PreviewTextInput"/>
                <EventSetter Event="LostFocus" Handler="RecoveryCoefficient_LostFocus"/>
              </Style>
            </DataGridTextColumn.CellStyle>
          </DataGridTextColumn>

          <DataGridTextColumn Header="Антагонисты" 
            Binding="{Binding AntagonistActions, Converter={StaticResource ListToStringConverter}}" 
            IsReadOnly="True" 
            Width="*">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <EventSetter Event="MouseDown" Handler="AntagonistCell_MouseDown"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
            <DataGridTextColumn.HeaderStyle>
              <Style TargetType="DataGridColumnHeader">
                <Setter Property="ToolTip">
                  <Setter.Value>
                    <TextBlock TextWrapping="Wrap" Width="200">
                      Список ID действий-антагонистов, которые несовместимы с данным действием.
                      При активации данного действия, все указанные здесь действия будут деактивированы.
                      ДБ клик открывает окно ввода.
                    </TextBlock>
                  </Setter.Value>
                </Setter>
              </Style>
            </DataGridTextColumn.HeaderStyle>
          </DataGridTextColumn>

        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <!-- Кнопки управления -->
    <Grid Grid.Row="3" Margin="0,0,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <!-- Текст предупреждения -->
      <TextBlock Grid.Column="0"
                 Text="{Binding PulseWarningMessage}"
                 Foreground="{Binding WarningMessageColor}"
                 FontStyle="Italic"
                 VerticalAlignment="Center"
                 Margin="8,0,0,0"
                 TextWrapping="Wrap"
                 Visibility="{Binding IsEditingEnabled, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>

      <!-- Кнопки -->
      <StackPanel Grid.Column="1"
                  Orientation="Horizontal" 
                  HorizontalAlignment="Right">
        <Button Content="Очистить"
                Command="{Binding RemoveAllCommand}"
                IsEnabled="{Binding IsEditingEnabled}"
                Style="{StaticResource RedButtonStyle}"
                ToolTip="Удалить все данные из таблицы"/>
        <Button Content="Сохранить" 
                Command="{Binding SaveCommand}"
                IsEnabled="{Binding IsEditingEnabled}"
                Style="{StaticResource ActionButtonStyle}"
                ToolTip="Сохранить данные"/>
      </StackPanel>
    </Grid>

  </Grid>

</UserControl>
