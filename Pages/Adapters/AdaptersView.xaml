<UserControl x:Class="AIStudio.Pages.Adapters.AdaptersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="200"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Зарегистрированные пакеты среды" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8"/>
    <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,8" Foreground="#FF555555"
               Text="{Binding AdaptersCatalogHint}"/>

    <Grid Grid.Row="2">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <Border Grid.Row="0" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,8" Background="#FFF5F5F5">
        <WrapPanel>
          <Button Content="Обновить список" Padding="10,6" Margin="0,0,8,4" Command="{Binding RefreshCommand}"/>
          <Button Content="Создать пакет…" Padding="10,6" Margin="0,0,8,4" Command="{Binding CreateFromDemoCommand}"
                  ToolTip="Manifest, каталог сборки host (DLL) и регистрация в Adapters"/>
          <Button Content="Зарегистрировать из папки…" Padding="10,6" Margin="0,0,8,4" Command="{Binding InstallFromFolderCommand}"/>
          <Button Content="Зарегистрировать из ZIP…" Padding="10,6" Margin="0,0,8,4" Command="{Binding InstallFromZipCommand}"/>
          <Button Content="Проверить" Padding="10,6" Margin="0,0,8,4" Command="{Binding VerifyCommand}"/>
          <Button Content="Открыть каталог Adapters" Padding="10,6" Margin="0,0,0,4" Command="{Binding OpenAdaptersFolderCommand}"/>
        </WrapPanel>
      </Border>

      <DataGrid x:Name="AdaptersGrid" Grid.Row="1"
                ItemsSource="{Binding Items}" SelectedItem="{Binding Selected, Mode=TwoWay}"
                AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False"
                MouseDoubleClick="AdaptersGrid_MouseDoubleClick"
                PreviewKeyDown="AdaptersGrid_PreviewKeyDown">
        <DataGrid.Columns>
          <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="120"/>
          <DataGridTextColumn Header="Название" Binding="{Binding DisplayName}" Width="*"/>
          <DataGridTextColumn Header="Версия" Binding="{Binding Version}" Width="90"/>
          <DataGridTextColumn Header="Контракт" Binding="{Binding ContractVersion}" Width="70"/>
        </DataGrid.Columns>
      </DataGrid>
    </Grid>

    <Border Grid.Row="3" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Padding="8" Margin="0,8,0,0">
      <DockPanel>
        <TextBlock DockPanel.Dock="Top" Text="Отчёт проверки" FontWeight="SemiBold" Margin="0,0,0,6"/>
        <TextBox Text="{Binding ReportText, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="Transparent"/>
      </DockPanel>
    </Border>
  </Grid>
</UserControl>
