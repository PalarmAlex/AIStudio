<UserControl x:Class="AIStudio.Pages.Understanding.ThemeTypesView"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AIStudio.Pages.Understanding"
             xmlns:viewmodels="clr-namespace:AIStudio.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ThemeTypesViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">

  <UserControl.Resources>
    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
      <Setter Property="Background" Value="White"/>
      <Style.Triggers>
        <Trigger Property="Validation.HasError" Value="true">
          <Setter Property="Background" Value="#FFFDE6E6"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="BaseTextBoxStyle" TargetType="TextBox">
      <Setter Property="Background" Value="White"/>
      <Style.Triggers>
        <Trigger Property="Validation.HasError" Value="true">
          <Setter Property="Background" Value="#FFFDE6E6"/>
        </Trigger>
      </Style.Triggers>
    </Style>
  </UserControl.Resources>

  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="{Binding CurrentAgentTitle}"
               FontSize="18"
               FontWeight="SemiBold"
               HorizontalAlignment="Center"
               Margin="0,0,0,5"
               Foreground="#FF333333"/>

    <TextBlock Grid.Row="1"
               HorizontalAlignment="Left"
               Margin="0,0,0,10"
               Foreground="#FF333333"
               TextWrapping="Wrap"
               Height="Auto">
      <Run Text="{Binding CurrentAgentDescription.Text}"/>
      <Hyperlink Command="{Binding CurrentAgentDescription.OpenLinkCommand}">
        <Run Text="{Binding CurrentAgentDescription.LinkText}"/>
      </Hyperlink>
    </TextBlock>

    <StackPanel Grid.Row="2" Margin="0,0,0,8">
      <TextBlock Text="Темы мышления привязанные к событиям агента"
                 FontWeight="SemiBold"
                 Foreground="#FF333333"
                 Margin="0,0,0,4"/>
      <Border BorderBrush="#FFE0E0E0"
              BorderThickness="1"
              CornerRadius="4"
              Padding="8,6"
              Background="#FFF5F5F5">
        <TextBlock Text="{Binding ReservedThemeTypesText}"
                   Foreground="#FF333333"
                   TextWrapping="Wrap"
                   FontFamily="Consolas"
                   FontSize="13"/>
      </Border>
    </StackPanel>

    <Border Grid.Row="3"
            BorderBrush="#FFE0E0E0"
            BorderThickness="1"
            CornerRadius="4"
            Margin="0,0,0,8"
            IsEnabled="{Binding IsEditingEnabled}">
      <DataGrid ItemsSource="{Binding ThemeTypes}"
                x:Name="ThemesGrid"
                IsEnabled="True"
                IsReadOnly="{Binding IsReadOnlyMode}"
                AutoGenerateColumns="False"
                CanUserAddRows="{Binding IsEditingEnabled}"
                CanUserDeleteRows="{Binding IsEditingEnabled}"
                SelectionMode="Extended"
                BorderThickness="0"
                Background="White"
                RowBackground="#FFF9F9F9"
                AlternatingRowBackground="White"
                GridLinesVisibility="None"
                VerticalScrollBarVisibility="Auto"
                HorizontalScrollBarVisibility="Auto"
                RowEditEnding="DataGrid_RowEditEnding"
                AddingNewItem="DataGrid_AddingNewItem"
                PreviewKeyDown="ThemesGrid_PreviewKeyDown">
        <DataGrid.Columns>
          <DataGridTextColumn Header="ID" Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}" Width="60" IsReadOnly="True"/>
          <DataGridTextColumn Header="Описание" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*" MinWidth="200">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="TextBlock">
                <Setter Property="TextWrapping" Value="Wrap"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="Margin" Value="4,2"/>
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTextColumn Header="Вес" Binding="{Binding DefaultWeight, UpdateSourceTrigger=PropertyChanged}" Width="50" MinWidth="50"/>
        </DataGrid.Columns>
      </DataGrid>
    </Border>

    <Grid Grid.Row="4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0"
                 Text="{Binding PulseWarningMessage}"
                 Foreground="{Binding WarningMessageColor}"
                 FontStyle="Italic"
                 VerticalAlignment="Center"
                 Margin="8,0,0,0"
                 TextWrapping="Wrap">
        <TextBlock.Style>
          <Style TargetType="TextBlock">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
              <DataTrigger Binding="{Binding IsEditingEnabled}" Value="True">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>

      <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="Очистить"
                Command="{Binding ClearCommand}"
                IsEnabled="{Binding IsEditingEnabled}"
                Style="{StaticResource RedButtonStyle}"
                ToolTip="Очистить описания всех типов тем в таблице и сохранить"
                Margin="0,0,8,0"/>
        <Button Content="Сохранить"
                Command="{Binding SaveCommand}"
                IsEnabled="{Binding IsEditingEnabled}"
                Style="{StaticResource ActionButtonStyle}"
                ToolTip="Сохранить типы тем в theme_types.dat"
                PreviewMouseLeftButtonDown="SaveButton_PreviewMouseLeftButtonDown"/>
      </StackPanel>
    </Grid>
  </Grid>
</UserControl>
