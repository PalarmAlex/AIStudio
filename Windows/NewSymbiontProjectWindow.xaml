<Window x:Class="AIStudio.Windows.NewSymbiontProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый проект симбионта" Height="340" Width="560"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,12"
               Text="Будет создан каталог данных гомеостаза симбионта. Тип среды (рецепты и триггеры) можно указать сейчас или добавить позже в Settings.xml."/>

    <CheckBox Grid.Row="1" x:Name="IncludeEnvironmentCheckBox" Margin="0,0,0,10"
              Content="Указать тип среды из зарегистрированного пакета"
              Checked="OnIncludeEnvironmentChanged" Unchecked="OnIncludeEnvironmentChanged"/>

    <TextBlock Grid.Row="2" Text="Тип среды (зарегистрированный пакет):" FontWeight="SemiBold" Margin="0,0,0,6"/>

    <ComboBox Grid.Row="3" x:Name="AdapterCombo" DisplayMemberPath="DisplayName"
              MinHeight="28" VerticalContentAlignment="Center" Margin="0,0,0,8"/>

    <TextBlock Grid.Row="4" x:Name="HintText" TextWrapping="Wrap" Foreground="#FF555555" Margin="0,0,0,12"
               Text="Без типа среды доступны гомеостаз, пульт оператора и виртуальные тесты. Меню «Среда» — после регистрации пакета и указания AdapterId."/>

    <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Далее…" Width="100" Margin="0,0,8,0" IsDefault="True" Click="OnNextClick"/>
      <Button Content="Отмена" Width="90" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
