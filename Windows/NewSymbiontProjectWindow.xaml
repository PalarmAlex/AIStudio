<Window x:Class="AIStudio.Windows.NewSymbiontProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый проект симбионта" Height="280" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,12"
               Text="Выберите адаптер среды. BootData проекта будет скопирован из установленного пакета в %ProgramData%\ISIDA\Adapters."/>

    <TextBlock Grid.Row="1" Text="Адаптер:" FontWeight="SemiBold" Margin="0,0,0,6"/>

    <ComboBox Grid.Row="2" x:Name="AdapterCombo" DisplayMemberPath="DisplayName"
              MinHeight="28" VerticalContentAlignment="Center" Margin="0,0,0,16"/>

    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Далее…" Width="100" Margin="0,0,8,0" IsDefault="True" Click="OnNextClick"/>
      <Button Content="Отмена" Width="90" IsCancel="True"/>
    </StackPanel>
  </Grid>
</Window>
